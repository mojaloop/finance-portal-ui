apiVersion: v1
kind: Service
metadata:
  name: {{ template "admin-portal-web-ui.fullname" . }}
  labels:
    app: {{ template "admin-portal-web-ui.name" . }}
    chart: {{ template "admin-portal-web-ui.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "admin-portal-web-ui.name" . }}
    release: {{ .Release.Name }}
