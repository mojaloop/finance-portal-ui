apiVersion: v1
kind: Service
metadata:
  name: {{ template "admin-portal-frontend.fullname" . }}
  labels:
    app: {{ template "admin-portal-frontend.name" . }}
    chart: {{ template "admin-portal-frontend.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "admin-portal-frontend.name" . }}
    release: {{ .Release.Name }}
