import React from 'react';
import Button from '@material-ui/core/Button';
import Card from '@material-ui/core/Card';

function ForexRateEndDateOption() {
  return (
    <Card>
      <Button
        variant="contained"
        color="primary"
      >
        Commit
      </Button>
    </Card>
  );
}

export default ForexRateEndDateOption;
